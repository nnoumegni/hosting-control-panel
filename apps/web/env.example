# Hosting Control Panel Web Application Environment Example
# Copy to .env.local (or another .env.* file) and adjust values before running the app.

# =============================================================================
# Application Configuration
# =============================================================================

# Node environment (development, production, test)
NODE_ENV=development

# Server hostname (default: localhost)
HOSTNAME=localhost

# Server port (default: 5010)
PORT=5010

# =============================================================================
# API Configuration
# =============================================================================

# API base URL - This is the backend API endpoint
# In development, this typically points to the API server running on port 4000
# In production, this should point to your production API endpoint
NEXT_PUBLIC_API_BASE_URL=https://localhost:4000/api

# =============================================================================
# HTTPS/TLS Configuration (Optional)
# =============================================================================

# To enable HTTPS in development/production, provide paths to your SSL certificate and private key
# If not provided, the server will run in HTTP mode
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key
# SSL_CA_PATH=/path/to/ca-bundle.crt  # Optional: Certificate Authority chain

# =============================================================================
# Optional: Themes and Apps Configuration
# =============================================================================

# App ID for themes and apps integration (optional)
# NEXT_PUBLIC_APP_ID=com.mesdoh.app

# =============================================================================
# Test Credentials (Development Only)
# =============================================================================

# NOTE: These credentials are only used in development mode (NODE_ENV=development)
# The API server creates an in-memory admin user with these credentials for testing.
# 
# Default test credentials:
#   Username: admin
#   Password: (set via DEV_ADMIN_PASSWORD in API .env, or defaults to 'ChangeMe123!')
#   Email: admin@example.com
#   Role: superadmin
#
# To use custom test credentials, set DEV_ADMIN_PASSWORD in the API's .env file:
#   DEV_ADMIN_PASSWORD=YourSecurePassword123!
#
# IMPORTANT: Never use these credentials in production!
# In production, use a proper authentication system with MongoDB-backed user storage.

# =============================================================================
# Quick Start Guide
# =============================================================================

# 1. Copy this file to .env.local:
#    cp env.example .env.local
#
# 2. Update NEXT_PUBLIC_API_BASE_URL to match your API server URL
#
# 3. (Optional) Configure SSL certificates for HTTPS
#
# 4. Start the development server:
#    yarn dev
#
# 5. Access the application:
#    - HTTP: http://localhost:5010
#    - HTTPS: https://localhost:5010 (if SSL configured)
#
# 6. Login with test credentials:
#    - Username: admin
#    - Password: ChangeMe123! (or your custom DEV_ADMIN_PASSWORD)

