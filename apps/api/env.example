# Hosting Control Panel API environment example
# Copy to .env.local (or another .env.* file) and adjust values before running the API.

NODE_ENV=development
PORT=4000
MONGODB_URI=mongodb://localhost:27017/hosting-control-panel
AWS_REGION=us-east-1
# Replace the placeholders below with actual RSA keys. Newlines must be escaped as \n.
JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nREPLACE_ME\n-----END PUBLIC KEY-----
JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----
JWT_ISSUER=hosting-control-panel
JWT_AUDIENCE=dashboard,api
AUTH_ACCESS_TOKEN_TTL=900
AUTH_REFRESH_TOKEN_TTL=604800
LOG_LEVEL=info
# Optional: configure AWS Security Group / Network ACL synchronization
FIREWALL_SECURITY_GROUP_ID=
FIREWALL_NETWORK_ACL_ID=
# Optional: passphrase used to encrypt stored AWS credentials (minimum 16 chars)
FIREWALL_CREDENTIAL_PASSPHRASE=

# WebSocket Agent Configuration
# WebSocket agents use the AWS secret access key for authentication
# The WebSocket URL is automatically generated using the instance's public IP
# No additional configuration needed - works out of the box!

# HTTPS/TLS Configuration (optional)
# To enable HTTPS, provide paths to your SSL certificate and private key
# If not provided, the server will run in HTTP mode
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key
# SSL_CA_PATH=/path/to/ca-bundle.crt  # Optional: Certificate Authority chain

